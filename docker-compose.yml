services:
  mongo:
    image: mongo:6.0
    container_name: mongo_db
    restart: always
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: todolist

  backend:
    build: .
    container_name: backend_app
    ports:
      - "8080:8080"
    depends_on:
      - mongo
